import { setModal, modalSettings } from './modal';
import { setTabs, mainTabSettings, portfolioTabSettings } from './tabs';
import { setTrack, portfolioTrackSettings, mainTrackSettings } from './track';

export { 
    setModal,
    modalSettings,
    setTabs,
    mainTabSettings,
    portfolioTabSettings,
    setTrack,
    portfolioTrackSettings,
    mainTrackSettings
}