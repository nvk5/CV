const mainTrackSettings = {
    trackSelector: '.nav .track',
    activeBtnSelector: '.nav__link.active'
}

const portfolioTrackSettings = {
    trackSelector: '.portfolio__tabs .track',
    activeBtnSelector: '.portfolio__tab-btn.active'
}

export { mainTrackSettings, portfolioTrackSettings };